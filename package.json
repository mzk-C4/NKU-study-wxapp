{
  "name": "nkustudy-wxapp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "check:miniprogram": "node scripts/check-miniprogram.js",
    "test:client-contract": "node --test test/*.test.js",
    "verify:quick": "npm run check:miniprogram",
    "verify:local": "npm test",
    "verify:external": "node scripts/release-preflight.js",
    "devtools:open": "node scripts/wechat-devtools.js open --project . --lang zh",
    "devtools:login": "node scripts/wechat-devtools.js islogin --project . --lang zh",
    "devtools:preview": "node scripts/wechat-devtools.js preview --project . --lang zh",
    "release:check": "npm run verify:local && npm run verify:external",
    "devtools:upload": "npm run release:check && node scripts/wechat-devtools.js upload --project . --lang zh",
    "test": "npm --prefix server test && npm run test:client-contract && npm run check:miniprogram"
  },
  "engines": { "node": ">=18" }
}
